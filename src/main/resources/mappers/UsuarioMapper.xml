<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="edu.eci.cvds..dao.mybatis.mappers.UsuarioMapper">
      <select parameterType="map" id="consultarUsuario" resultMap="UsuarioResult">
        SELECT 
        i.id ,
        i.name ,
        i.password,
        i.email ,
        i.status  ,
        i.userType  ,  
        from Usuarios as i 
    </select>

    <insert id="insertarUsuario" parameterType="map">
        INSERT INTO Usuarios
        VALUES( #{id},#{name},#{password},#{email},#{status},#{userType}) ;
    </insert>

    <resultMap type='Usuario' id='UsuarioResult'>
        <id property='id' column='id'/>
        <result property='name' column='name'/>
        <result property='password' column='password'/>
        <result property='email' column='email'/>
        <result property='status' column='status'/>
        <result property='userType' column='userType'/> 
        <association property='Usuario' javaType = 'TipoUsuario' resultMap='edu.eci.cvds.dao.mybatis.mappers.UsuarioMapper.consultarUsuario' columnPrefix='tipoUsuario_'></association>
    </resultMap
          	
</mapper>