<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="edu.eci.cvds..dao.mybatis.mappers.UserMapper">
      <select parameterType="map" id="consultarUsuario" resultMap="UsuarioResult">
        SELECT 
        i.id ,
        i.name ,
        i.password,
        i.email ,
        i.status  ,
        i.userType  ,  
        from Users as i 
    </select>

    <insert id="insertarUsuario" parameterType="map">
        INSERT INTO Users
        VALUES(#{name},#{password},#{email},#{status},#{userType}) ;
    </insert>

    <resultMap type='User' id='UsuarioResult'>
        <id property='id' column='id'/>
        <result property='name' column='name'/>
        <result property='password' column='password'/>
        <result property='email' column='email'/>
        <result property='status' column='status'/>
        <result property='userType' column='userType'/> 
        <association property='User' javaType = 'TipoUsuario' resultMap='edu.eci.cvds.dao.mybatis.mappers.UserMapper.consultarUsuario' columnPrefix='tipoUsuario_'></association>
    </resultMap>
          	
</mapper>